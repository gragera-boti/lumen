# 2026-02-23

## Typography Overhaul — Complete
- Replaced system serif fonts with 10 custom Google Fonts (~1.9MB OFL-licensed)
- Font files in `Lumen/Resources/Fonts/`, registered in `Info.plist` (`UIAppFonts`)
- `AffirmationFontStyle` enum: playfair, cormorant, caveat, dancing, abril, josefin, zilla, righteous, rounded, heavy
- Weighted random assignment: 40% Playfair, 20% Cormorant, 10% each Zilla/Abril/Rounded/Josefin — deterministic via `affirmation.id.hashValue`
- Base sizes: 42/34/28/23pt by text length
- Legacy style names auto-map via `AffirmationFontStyle.from(_:)`
- `cardFont(textLength:)` is the central sizing method; `previewFont(size:)` for picker previews

## Branch Merge
- `redesign/immersive-ui` merged → `main` (commit `314a59f`), pushed
- `main` safe rollback: `b001c99`
- Build succeeded, installed on device `00008150-001C51A81AB8401C`

## Immersive UI Redesign — Summary
- Universal dark ambient background (`AmbientBackground` view + `.ambientBackground()` modifier)
- Glassmorphic cards (`CategoryCardView`)
- Theme tokens: `ambientDark`, `ambientMid`, `ambientLight`, `glassBackground`, `glassBorder`
- No light mode — forced `.preferredColorScheme(.dark)` at app root
- Card customization: `CardCustomization` SwiftData model, `CardEditorView` with procedural + AI dual mode
- AI background images cached to `Documents/CardCustomizations/`
- Detail view action bar matches Feed; Slideshow minimal controls only
- Sensitive content filter refreshes live in Explore tab via `@Query` + `.task(id:)`
