# 2026-02-19

## ASC IAP Products Created (via API)
- Used Mise API key (7LBYRCA3YG, Admin) — the Lumen key .p8 was never saved
- Subscription Group: **Lumen Pro** (ID: `21941237`)
- Monthly: `lumen.premium.monthly` (ID: `6759404315`) — 1 month, $3.99 target
- Yearly: `lumen.premium.yearly` (ID: `6759401344`) — 1 year, $19.99 target
- Lifetime: `lumen.premium.lifetime` (ID: `6759401345`) — non-consumable, **$49.99 set via API**
- EN + ES localizations added for all 3 products + group
- Group localizations: EN + ES (custom app name: "Lumen")

### Pricing Status
- **Lifetime: $49.99 SET** via `inAppPurchasePriceSchedules` API
- **Monthly + Yearly: FAILED** — `subscriptionPrices` API returns 500 consistently
  - Likely Apple API bug or needs "Paid Applications" agreement active
  - Will set via browser when available

### RevenueCat Status
- IAP key exists: `SubscriptionKey_4BMPU6L45L.p8` in Downloads
- ASC API key for RC: `AuthKey_7LBYRCA3YG.p8` (Mise key, Admin)
- RC dashboard needs: upload both .p8 keys, update product IDs from test (`monthly`/`yearly`/`lifetime`) to real (`lumen.premium.monthly`/etc.)
- All needs browser

### Pending (needs browser)
1. Set Monthly ($3.99) and Yearly ($19.99) subscription prices in ASC
2. Upload IAP .p8 key to RevenueCat
3. Upload ASC API .p8 key to RevenueCat  
4. Update RC product IDs to match ASC
5. Swap RC test API key to production
