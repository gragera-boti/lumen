# 2026-02-22

## Major Session: Lumen Redesign + Features

### AGENTS.md Compliance Pass
- Migrated all 16 test files from XCTest → Swift Testing (@Suite, @Test, #expect)
- Removed @unchecked Sendable from all services; actors for stateful, structs for stateless
- DocC comments on all 14 service protocols
- Added #Preview to all 26 view files
- Accessibility labels across all interactive elements
- Centralized Logger+Lumen.swift
- Documentation.md created

### Card Customization Feature
- New CardCustomization SwiftData model (per-affirmation overrides)
- CardEditorView sheet: background (procedural/AI/My Backgrounds), typography, text
- Wired into Feed, Favorites, Slideshow, Detail, History, Explore CategoryFeed
- CardCustomizationService + tests
- Background images cached to Documents/CardCustomizations/ on save
- cachedImagePath + savedThemeId fields for persistent background storage
- "My Backgrounds" picker in editor loads from themes/generated and themes/ai dirs

### Immersive UI Redesign (branch: redesign/immersive-ui → merged to main)
- Universal dark ambient background (ambientDark #0A0E1A, ambientMid #131B2E)
- .preferredColorScheme(.dark) forced at app level — no light mode
- .ambientBackground() modifier applied to ALL screens
- Glassmorphic cards (glassBackground, glassBorder) for Explore + Favorites
- Dark toolbar color scheme everywhere
- Settings uses .scrollContentBackground(.hidden) over ambient gradient

### Custom Typography Overhaul
- 8 bundled OFL fonts (~1.9MB): Playfair Display, Cormorant Garamond, Caveat, Dancing Script, Abril Fatface, Josefin Sans, Zilla Slab, Righteous
- Kept SF Rounded + SF Pro Heavy as system options
- Base sizes bumped: 42pt (short), 34pt (medium), 28pt (long), 23pt (very long)
- Playfair Display is the signature/default font (40% weight in random assignment)
- Legacy font style names auto-map to new styles via AffirmationFontStyle.from()
- PostScript names: PlayfairDisplayRoman-Bold, CormorantGaramond-Bold, CaveatRoman-Bold, DancingScript-Bold, AbrilFatface-Regular, JosefinSansRoman-Regular, ZillaSlab-Bold, Righteous-Regular

### Bug Fixes
- Settings crash: ModelContainer now auto-resets store on migration failure
- Views refresh after saving customization (regenerateBackground compares updatedAt)
- Sensitive content filter refreshes in Explore without app restart (.task(id:))
- Back button invisible on empty category feed (adapts color to content state)
- Detail view matches Feed action bar style (icon+caption VStack)
- Detail view hides tab/nav bar like Feed
- Smooth launch transition (splash fades out over 0.4s, content renders underneath)
- No loading flash on tab switch (only first load shows spinner)
- Tab/nav bar appearance in ContentView.init() instead of .onAppear
- Background aspect ratio: all display sites use .fill + frame + clipped
- CardEditor procedural preview now generates at 1080x1920 (was 512x512)
- Removed Data & Privacy section from Settings

### Pending Issues
- AI backgrounds still vertically stretched on display — SD generates 512x512, need to generate larger or post-process to portrait aspect ratio
- iCloud sync code ready but container not registered in ASC (cloudKitDatabase: .none)
- Alberto wants AI backgrounds generated at larger/portrait size to crop correctly

### Git
- Repo: github.com/gragera-boti/lumen (private)
- All on main branch now (redesign branch merged)
- Device: iPhone 17 Pro Max (00008150-001C51A81AB8401C)
