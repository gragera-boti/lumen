import Foundation

enum ThemeType: String, Codable {
    case curatedImage = "CURATED_IMAGE"
    case gradient = "GRADIENT"
    case generatedImage = "GENERATED_IMAGE"
    case customPhoto = "CUSTOM_PHOTO"
}
