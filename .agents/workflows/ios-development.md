---
description: iOS Development and Automated Code Review Lifecycle
---

# iOS Implementation & Review Workflow

When instructed to develop or fix a feature in this repository, follow this exact sequence:

1. **Plan Phase**
   - Research the requested feature across the codebase.
   - Draft an `implementation_plan` artifact detailing the proposed solution.
   - Wait for the user to approve the plan before writing any code.

2. **Execution Phase**
   - Implement the approved plan, abiding strictly by the rules in `IOS_ENGINEER.md`.
   - Automatically modify existing tests and create new ones to comprehensively cover whatever feature you are working on.
   - Ensure that the implementation includes excellent test coverage (unit tests and/or UI tests as applicable).
   
3. **Internal Code Review Phase**
   - Stop and read `IOS_REVIEWER.md`.
   - Adopt the strict iOS Reviewer persona and scrutinize the code you just wrote.
   - Document any architectural flaws, missing tests, potential retain cycles, or SwiftUI inefficiencies.

4. **Iteration Phase**
   - Take the feedback generated by the Reviewer persona and immediately refactor the code to fix the issues.
   - Repeat the internal "Review" and "Iterate" steps until the Reviewer persona is 100% satisfied with the code quality and test coverage.

5. **User Verification Phase**
   - Run the tests as part of the verification plan.
   - Generate a `walkthrough` artifact demonstrating what was done and tested.
   - Ask the user to review the final iteration to see if they are satisfied with the result.
